# React Vite App

Quick start:

```powershell
cd "Front End/react-vite-app"
npm install
npm run dev
```

- `dev`: starts Vite dev server
- `build`: builds for production
- `preview`: preview production build
